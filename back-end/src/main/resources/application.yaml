spring:
  application:
    name: spring-security-demo-1
  datasource:
    password: root
    url: jdbc:mysql://localhost/public?useSSL=false&serverTimezone=America/Chicago
    username: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL5Dialect
        format_sql: true
    show-sql: false
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
server:
  port: 9000
subo8:
  app:
    jwtSecret: SecretKey!
    jwtExpirationMs: '900000'